node_modules/
dist/
.wrangler/
.dev.vars
.env*
!.env.example
playwright-report/
test-results/
*.log
*.tsbuildinfo

# CI installs with bun and no lockfile is tracked; keep a locally generated
# npm lockfile out of the repository so it cannot pin a different resolution.
package-lock.json
bun.lock
