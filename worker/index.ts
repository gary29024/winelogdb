import { Hono } from 'hono';
import { cors } from 'hono/cors';
import { GoogleGenerativeAI, SchemaType } from '@google/generative-ai';
import { requireSession } from '../src/lib/auth/session';
import { createObjectKey } from '../src/lib/r2/keys';
import { wineInputSchema } from '../src/lib/db/schema';
import { dimensionsSchema, validateBatch } from '../src/features/uploads/validation';
import { parseRecognition } from '../src/features/recognition/schema';

type Bindings={DB:D1Database;WINE_IMAGES:R2Bucket;ASSETS:Fetcher;GEMINI_API_KEY:string;AUTH_SECRET:string;APP_URL:string;MAX_FILE_BYTES?:string;MAX_BATCH_FILES?:string};
type Variables={userId:string};
const app=new Hono<{Bindings:Bindings;Variables:Variables}>();
app.use('/api/*',cors({origin:(origin,c)=>origin===c.env.APP_URL?origin:null,credentials:true}));
app.use('/api/*',async(c,next)=>{try{const s=await requireSession(c.req.header('Authorization'),c.env.AUTH_SECRET);c.set('userId',s.userId);await next();}catch{return c.json({error:'Unauthorized'},401)}});
const mapWine=(r:Record<string,unknown>)=>({id:r.id,ownerId:r.owner_id,producer:r.producer,wineName:r.wine_name,vintage:r.vintage,country:r.country,region:r.region,appellation:r.appellation,grapes:JSON.parse(String(r.grapes_json)),wineStyle:r.wine_style,alcoholPercentage:r.alcohol_percentage,tastingNotes:r.tasting_notes,rating:r.rating,tastingDate:r.tasting_date,event:r.event,venue:r.venue,price:r.price,currency:r.currency,tags:JSON.parse(String(r.tags_json)),recognitionStatus:r.recognition_status,recognitionConfidence:r.recognition_confidence,createdAt:r.created_at,updatedAt:r.updated_at});
app.get('/api/wines',async c=>{const q=c.req.query(); const args:unknown[]=[c.get('userId')]; let where='owner_id=?'; const filters:[string,string][]=[['vintage','vintage'],['country','country'],['region','region'],['style','wine_style'],['event','event'],['tastingDate','tasting_date']]; for(const [key,col] of filters)if(q[key]){where+=` AND ${col}=?`;args.push(q[key])} if(q.rating){where+=' AND rating>=?';args.push(Number(q.rating))} if(q.grape){where+=' AND EXISTS (SELECT 1 FROM json_each(grapes_json) WHERE value=?)';args.push(q.grape)} if(q.query){where+=` AND id IN (SELECT wine_id FROM wine_search WHERE wine_search MATCH ? AND owner_id=?)`;args.push(q.query.replace(/[^\p{L}\p{N}\s]/gu,' ')+'*',c.get('userId'))} const orders:Record<string,string>={newest:'created_at DESC',oldest:'created_at ASC',rating:'rating DESC',producer:'producer COLLATE NOCASE',vintage:'vintage DESC'}; const limit=Math.min(Number(q.limit)||24,100),offset=Math.max(Number(q.offset)||0,0);args.push(limit,offset);const rows=await c.env.DB.prepare(`SELECT * FROM wines WHERE ${where} ORDER BY ${orders[q.sort]||orders.newest} LIMIT ? OFFSET ?`).bind(...args).all();return c.json({items:rows.results.map(r=>mapWine(r as Record<string,unknown>)),nextOffset:rows.results.length===limit?offset+limit:null})});
app.get('/api/wines/:id',async c=>{const row=await c.env.DB.prepare('SELECT * FROM wines WHERE id=? AND owner_id=?').bind(c.req.param('id'),c.get('userId')).first();return row?c.json(mapWine(row)):c.json({error:'Not found'},404)});
app.post('/api/wines',async c=>{const parsed=wineInputSchema.safeParse(await c.req.json());if(!parsed.success)return c.json({error:'Invalid wine',issues:parsed.error.issues},400);const w=parsed.data,id=crypto.randomUUID(),now=new Date().toISOString();await c.env.DB.prepare(`INSERT INTO wines(id,owner_id,producer,wine_name,vintage,country,region,appellation,grapes_json,wine_style,alcohol_percentage,tasting_notes,rating,tasting_date,event,venue,price,currency,tags_json,recognition_status,recognition_confidence,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).bind(id,c.get('userId'),w.producer,w.wineName,w.vintage,w.country,w.region,w.appellation,JSON.stringify(w.grapes),w.wineStyle,w.alcoholPercentage,w.tastingNotes,w.rating,w.tastingDate,w.event,w.venue,w.price,w.currency,JSON.stringify(w.tags),w.recognitionStatus,w.recognitionConfidence,now,now).run();return c.json({id},201)});
app.put('/api/wines/:id',async c=>{const w=wineInputSchema.safeParse(await c.req.json());if(!w.success)return c.json({error:'Invalid wine',issues:w.error.issues},400);const x=w.data,res=await c.env.DB.prepare(`UPDATE wines SET producer=?,wine_name=?,vintage=?,country=?,region=?,appellation=?,grapes_json=?,wine_style=?,alcohol_percentage=?,tasting_notes=?,rating=?,tasting_date=?,event=?,venue=?,price=?,currency=?,tags_json=?,recognition_status=?,recognition_confidence=?,updated_at=? WHERE id=? AND owner_id=?`).bind(x.producer,x.wineName,x.vintage,x.country,x.region,x.appellation,JSON.stringify(x.grapes),x.wineStyle,x.alcoholPercentage,x.tastingNotes,x.rating,x.tastingDate,x.event,x.venue,x.price,x.currency,JSON.stringify(x.tags),x.recognitionStatus,x.recognitionConfidence,new Date().toISOString(),c.req.param('id'),c.get('userId')).run();return res.meta.changes?c.json({ok:true}):c.json({error:'Not found'},404)});
app.delete('/api/wines/:id',async c=>{const id=c.req.param('id'),owner=c.get('userId');const images=await c.env.DB.prepare('SELECT object_key FROM wine_images WHERE wine_id=? AND owner_id=?').bind(id,owner).all<{object_key:string}>();const deleted=await c.env.DB.prepare('DELETE FROM wines WHERE id=? AND owner_id=?').bind(id,owner).run();if(!deleted.meta.changes)return c.json({error:'Not found'},404);for(const image of images.results){const refs=await c.env.DB.prepare('SELECT COUNT(*) n FROM wine_images WHERE object_key=? AND wine_id IS NOT NULL').bind(image.object_key).first<{n:number}>();if(!refs?.n){await c.env.WINE_IMAGES.delete(image.object_key);await c.env.DB.prepare('DELETE FROM wine_images WHERE object_key=? AND wine_id IS NULL').bind(image.object_key).run()}}return c.body(null,204)});
app.post('/api/uploads',async c=>{const form=await c.req.formData(),files=form.getAll('images').filter((x):x is File=>x instanceof File);try{validateBatch(files,{maxFiles:Number(c.env.MAX_BATCH_FILES)||12,maxBytes:Number(c.env.MAX_FILE_BYTES)||10485760,minDimension:300,maxDimension:12000})}catch(e){return c.json({error:(e as Error).message},400)}const dimensions=JSON.parse(String(form.get('dimensions')||'[]')) as unknown[];if(dimensions.length!==files.length)return c.json({error:'Dimensions required for every image'},400);const results=[];for(let i=0;i<files.length;i++){const dim=dimensionsSchema.safeParse(dimensions[i]);if(!dim.success){results.push({name:files[i].name,status:'failed',error:'Invalid image dimensions'});continue}const file=files[i],key=createObjectKey(c.get('userId'),file.type),imageId=crypto.randomUUID();try{await c.env.WINE_IMAGES.put(key,file.stream(),{httpMetadata:{contentType:file.type},customMetadata:{ownerId:c.get('userId')}});await c.env.DB.prepare(`INSERT INTO wine_images(id,owner_id,object_key,content_type,byte_size,width,height,upload_status,created_at) VALUES(?,?,?,?,?,?,?,'uploaded',?)`).bind(imageId,c.get('userId'),key,file.type,file.size,dim.data.width,dim.data.height,new Date().toISOString()).run();results.push({id:imageId,name:file.name,status:'uploaded',objectKey:key})}catch(e){await c.env.WINE_IMAGES.delete(key);results.push({name:file.name,status:'failed',error:(e as Error).message})}}return c.json({results},207)});
app.get('/api/images/:id',async c=>{const row=await c.env.DB.prepare('SELECT object_key FROM wine_images WHERE id=? AND owner_id=?').bind(c.req.param('id'),c.get('userId')).first<{object_key:string}>();if(!row)return c.json({error:'Not found'},404);const obj=await c.env.WINE_IMAGES.get(row.object_key);if(!obj)return c.json({error:'Not found'},404);return new Response(obj.body,{headers:{'Content-Type':obj.httpMetadata?.contentType||'application/octet-stream','Cache-Control':'private, max-age=300','Content-Security-Policy':"default-src 'none'"}})});
app.post('/api/recognition/:id',async c=>{const image=await c.env.DB.prepare('SELECT * FROM wine_images WHERE id=? AND owner_id=?').bind(c.req.param('id'),c.get('userId')).first<Record<string,unknown>>();if(!image)return c.json({error:'Not found'},404);await c.env.DB.prepare("UPDATE wine_images SET recognition_status='processing',error=NULL WHERE id=?").bind(image.id).run();try{const obj=await c.env.WINE_IMAGES.get(String(image.object_key));if(!obj)throw new Error('Image missing');const bytes=new Uint8Array(await obj.arrayBuffer());let binary='';for(let i=0;i<bytes.length;i+=32768)binary+=String.fromCharCode(...bytes.subarray(i,i+32768));const genAI=new GoogleGenerativeAI(c.env.GEMINI_API_KEY);const model=genAI.getGenerativeModel({model:'gemini-2.0-flash',generationConfig:{responseMimeType:'application/json',responseSchema:{type:SchemaType.OBJECT,properties:{producer:{type:SchemaType.STRING,nullable:true},wineName:{type:SchemaType.STRING,nullable:true},vintage:{type:SchemaType.NUMBER,nullable:true},country:{type:SchemaType.STRING,nullable:true},region:{type:SchemaType.STRING,nullable:true},appellation:{type:SchemaType.STRING,nullable:true},grapes:{type:SchemaType.ARRAY,items:{type:SchemaType.STRING}},style:{type:SchemaType.STRING,nullable:true},alcoholPercentage:{type:SchemaType.NUMBER,nullable:true},confidence:{type:SchemaType.NUMBER}},required:['grapes','confidence']}}});let last:Error|undefined;for(let attempt=0;attempt<3;attempt++){try{const timeout=new Promise<never>((_,reject)=>setTimeout(()=>reject(new Error('Recognition timed out')),20000));const response=await Promise.race([model.generateContent(['Extract the wine label fields. Use null when unknown. Confidence is 0 to 1.',{inlineData:{data:btoa(binary),mimeType:String(image.content_type)}}]),timeout]);const result=parseRecognition(response.response.text());await c.env.DB.prepare("UPDATE wine_images SET recognition_status='review' WHERE id=?").bind(image.id).run();return c.json(result)}catch(e){last=e as Error;if(attempt<2)await new Promise(r=>setTimeout(r,500*2**attempt))}}throw last}catch(e){await c.env.DB.prepare("UPDATE wine_images SET recognition_status='failed',error=? WHERE id=?").bind((e as Error).message,image.id).run();return c.json({error:(e as Error).message},502)}});
app.all('*',c=>c.env.ASSETS.fetch(c.req.raw));
export default app;
