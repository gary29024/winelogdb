# Producer profile & range references

The producer page keeps the accepted research URLs available for audit, but presents them behind a compact disclosure. The summary shows both the number of reference URLs and the number of distinct websites so repeated pages from the same domain do not dominate the default view.
