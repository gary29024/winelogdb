# Catalogue refresh safety boundary

Producer profile fields may still be refreshed independently, but catalogue replacement is all-or-nothing. Temporary slice results never become the visible producer range until full coverage and integrity checks pass.
